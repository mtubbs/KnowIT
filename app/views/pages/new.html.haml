%h2
  Create a New Article
%p
  Here is where you can create a new Article aka a Solution.

  - form_for :page, @article, :url => { :action => "create" }, :html => {:class => "nifty_form"} do |f|
    %h2
      = f.label :title, "Title"    
    = f.text_field :title
    %h2
      = f.label :body, "Body"
    = f.text_area :body, :size => "60x12"
    %h2
      = f.label :url, "Link"
    = f.text_field :url
    %h2
      = label :create, "Save your work"
    = submit_tag "Create"
    %br/
    %br/
